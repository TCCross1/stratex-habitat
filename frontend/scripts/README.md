# Frontend validation scripts

Optional headless smoke (install `puppeteer-core` locally):
login as demo homeowner, open `/twin`, assert local Central Kentucky assets,
no Villa Horizon/Austin copy, no emergent flat-roof CDN request.
